# Base URL for the backend API.
# In development this is optional — Vite's dev server proxies "/api"
# requests to http://localhost:5000 (see vite.config.js).
# Set this explicitly for production builds pointing at a deployed backend.
VITE_API_BASE_URL=http://localhost:5000/api